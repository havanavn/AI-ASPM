org.gradle.parallel=true
org.gradle.caching=true
# Error Prone runs as a javac plugin and needs the compiler's internal packages
# exported. Forked compilation keeps those flags off the Gradle daemon itself.
org.gradle.jvmargs=-Xmx2g
