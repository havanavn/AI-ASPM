/**
 * Published contract of the schema-registry kernel module: Typed attribute schemas for custom fields and type registries.
 *
 * <p>Kernel module per DOC-02 section 6.2. Shell only at this stage; the implementation
 * is prompt 3.
 */
package aspm.kernel.schemaregistry.contract;
