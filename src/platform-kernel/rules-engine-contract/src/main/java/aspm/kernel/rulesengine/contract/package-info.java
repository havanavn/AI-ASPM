/**
 * Published contract of the rules-engine kernel module: Deterministic condition-action evaluation.
 *
 * <p>Kernel module per DOC-02 section 6.2. Shell only at this stage; the implementation
 * is prompt 3.
 */
package aspm.kernel.rulesengine.contract;
