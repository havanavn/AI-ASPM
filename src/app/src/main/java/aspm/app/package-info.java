/**
 * Application assembly for the modular monolith of ADR-003.
 *
 * <p>Runtime units per DOC-15 section 4 are configurations of this one artifact:
 * OPS-DEP-003 requires all three topologies to run the same artifact with the same code
 * paths, differing only in configuration and scale.
 */
package aspm.app;
