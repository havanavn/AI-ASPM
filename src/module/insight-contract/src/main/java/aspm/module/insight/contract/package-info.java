/**
 * Published contract of the insight module [projection], DOC-03 section 5.1 context 17.
 *
 * <p>Projection over events via :platform-events. Not a domain context (DOC-03 5.1 row 17).
 *
 * <p>Shell only at this stage. The aggregate, its invariants and its schema are
 * implemented in the prompt that owns this module.
 */
package aspm.module.insight.contract;
