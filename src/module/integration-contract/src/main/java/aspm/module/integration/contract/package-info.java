/**
 * Published contract of the integration module [generic], DOC-03 section 5.1 context 15.
 *
 * <p>External connector surface. The translating Anti-Corruption Layer sits in Ingestion, not here (DOC-03 5.3 row 4).
 *
 * <p>Shell only at this stage. The aggregate, its invariants and its schema are
 * implemented in the prompt that owns this module.
 */
package aspm.module.integration.contract;
