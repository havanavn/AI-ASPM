/**
 * Published contract of the knowledge module [supporting], DOC-03 section 5.1 context 10.
 *
 * <p>Publishes guidance referenced by finding class and checklist item.
 *
 * <p>Shell only at this stage. The aggregate, its invariants and its schema are
 * implemented in the prompt that owns this module.
 */
package aspm.module.knowledge.contract;
