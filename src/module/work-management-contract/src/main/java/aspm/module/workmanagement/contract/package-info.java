/**
 * Published contract of the work-management module [core], DOC-03 section 5.1 context 5.
 *
 * <p>Receives finding and assessment linkage by EVENT only (DOC-02 Figure 6.1 dashed edges). No compile dependency on vulnerability-management or assessment.
 *
 * <p>Shell only at this stage. The aggregate, its invariants and its schema are
 * implemented in the prompt that owns this module.
 */
package aspm.module.workmanagement.contract;
