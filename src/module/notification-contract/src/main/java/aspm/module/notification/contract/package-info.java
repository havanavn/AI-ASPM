/**
 * Published contract of the notification module [generic], DOC-03 section 5.1 context 13.
 *
 * <p>Event subscriber via :platform-events (DOC-02 7.3). No compile dependency on any publishing module. PRD-WRK-037: never a transition side effect.
 *
 * <p>Shell only at this stage. The aggregate, its invariants and its schema are
 * implemented in the prompt that owns this module.
 */
package aspm.module.notification.contract;
