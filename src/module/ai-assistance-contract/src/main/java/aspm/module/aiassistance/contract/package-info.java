/**
 * Published contract of the ai-assistance module [generic], DOC-03 section 5.1 context 16.
 *
 * <p>Separate Ways, read-only (DOC-03 5.3 row 14). PRD-AIC-021: no compile-time dependency on any domain module, and no write grant on any domain table.
 *
 * <p>Shell only at this stage. The aggregate, its invariants and its schema are
 * implemented in the prompt that owns this module.
 */
package aspm.module.aiassistance.contract;
