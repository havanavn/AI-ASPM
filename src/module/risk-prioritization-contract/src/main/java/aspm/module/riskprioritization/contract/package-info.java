/**
 * Published contract of the risk-prioritization module [core], DOC-03 section 5.1 context 3.
 *
 * <p>Customer-Supplier customer that READS three upstreams and computes; owns no source data (DOC-03 5.3 row 9).
 *
 * <p>Shell only at this stage. The aggregate, its invariants and its schema are
 * implemented in the prompt that owns this module.
 */
package aspm.module.riskprioritization.contract;
