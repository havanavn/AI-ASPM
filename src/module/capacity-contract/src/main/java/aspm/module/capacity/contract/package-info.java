/**
 * Published contract of the capacity module [supporting], DOC-03 section 5.1 context 8.
 *
 * <p>Customer-Supplier customer that reads work item assignment and transition history; does not write work state (DOC-03 5.3 row 11).
 *
 * <p>Shell only at this stage. The aggregate, its invariants and its schema are
 * implemented in the prompt that owns this module.
 */
package aspm.module.capacity.contract;
