/**
 * Published contract of the organization-scope module [supporting], DOC-03 section 5.1 context 6.
 *
 * <p>Publishes OrgNodeId and the immutable ScopeDescriptor. Depends on no domain module.
 *
 * <p>Shell only at this stage. The aggregate, its invariants and its schema are
 * implemented in the prompt that owns this module.
 */
package aspm.module.organizationscope.contract;
