// Shared Kernel per DOC-03 section 5.2: Tenant, ScopeDescriptor, Severity ordinal and
// identifier primitives. Deliberately tiny. Anything that grows here becomes coupling
// between every context, which is why it holds identifiers and value objects only.
