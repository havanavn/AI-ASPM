# GENERATED by aspm.deployment.KubernetesManifests from the DOC-15 runtime-unit model.
# Do not edit: KubernetesManifestsTest rewrites this file and fails on drift.
# Every NetworkPolicy, resource envelope and probe in the chart is read from here.
units:
  app:
    modelUnit: APPLICATION_TIER
    role: app
    servesHttp: true
    holdsDatabaseCredential: true
    placement: APPLICATION_POOL
    profile: CPU_BOUND_MODERATE_MEMORY
    readinessPath: /internal/health/ready
    livenessPath: /internal/health/live
    resources:
      cpuRequest: "500m"
      cpuLimit: "2"
      memoryRequest: 1Gi
      memoryLimit: 2Gi
    egress:
      - IDENTITY_PROVIDER
      - SECRETS_STORE
      - KEY_MANAGEMENT
  worker:
    modelUnit: GENERAL_WORKERS
    role: worker
    servesHttp: false
    holdsDatabaseCredential: true
    placement: WORKER_POOL
    profile: BALANCED
    readinessPath: /internal/health/ready
    livenessPath: /internal/health/live
    resources:
      cpuRequest: "250m"
      cpuLimit: "1"
      memoryRequest: 512Mi
      memoryLimit: 1Gi
    egress:
      - SECRETS_STORE
      - KEY_MANAGEMENT
      - CONNECTOR_ALLOWLIST
      - WEBHOOK_ALLOWLIST
      - MAIL_RELAY
      - MODEL_PROVIDER
notShipped:
  INGRESS: "provided by the cluster: Gateway API or an Ingress controller, TLS terminated there (ADR-057)"
  WEB_TIER: "served by the application tier: ADR-058 server-rendered shell plus the committed bundle"
  MATCH_WORKERS: "composition matching runs in the application tier today; a separate pool arrives with the match queue"
  PROJECTION_WORKERS: "read models are maintained in the write transaction today; a separate pool arrives with projection lag"
  SCHEDULER: "no in-process scheduler by design (OPS-DEP-007); the scanner container's tick and the worker's housekeeping stand in"
destinationClasses:
  - PUBLIC_INBOUND
  - IDENTITY_PROVIDER
  - SECRETS_STORE
  - KEY_MANAGEMENT
  - CONNECTOR_ALLOWLIST
  - WEBHOOK_ALLOWLIST
  - MAIL_RELAY
  - INTELLIGENCE_FEED
  - MODEL_PROVIDER
